# Rock-Scissors-Paper YOLO Fine-tuning
# Requires Python 3.8+

# Core packages
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0,<2.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0

# Dataset download (requests is usually already installed)
requests>=2.28.0

# Training monitor (optional)
# tensorboard>=2.13.0
# wandb>=0.15.0
